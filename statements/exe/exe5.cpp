//in ra tri tuyet doi cua so nguyen
#include<iostream>
#include<windows.h>
#include<math.h>

using namespace std;
int main(){
    int a, b;
    cin >> a >> b;
    cout << abs(a) << " " << abs(b) << endl;
    system("pause");
    return 0;
}