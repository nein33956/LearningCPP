//tinh can bac 2 cua mot so nguyen
#include<iostream>
#include<windows.h>
#include<math.h>

using namespace std;
int main(){
    int a, b;
    cin >> a >> b;
    cout << sqrt(a) << " " << sqrt(b) << endl;
    system("pause");
    return 0;
}